% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urc_mcmc2.R
\name{urc_mcmc2}
\alias{urc_mcmc2}
\title{Fit Underreported Count Models with Two Independent Samples via JAGS.}
\usage{
urc_mcmc2(
  x,
  thresh = 2,
  prior_lambda = "dgamma(0.1, 0.1)",
  prior_p = "dunif(0, 1)",
  prior_pi = "dunif(0, 1)",
  prior_c = "dunif(0, 1)",
  n_iter = 8000,
  n_chains = 2,
  n_burnin = 4000,
  inits = NULL,
  seed = 123
)
}
\arguments{
\item{x}{A named list containing the data for JAGS models. Must include:
'yobs' (observed count df), 'ystar' (true "unobserved" count df),
and 'yv' (validation set df). All dataframes are 2-dimensional.}

\item{thresh}{Numeric. Threshold for deciding between models when DICs are close. Default is \code{2}.}

\item{prior_lambda}{A prior distribution for the Poisson or ZIP rate parameter \code{lambda}.}

\item{prior_p}{A prior distribution for the reporting probability \code{p}.}

\item{prior_pi}{A prior distribution for the zero-inflation probability \code{pi} (ZIP only).}

\item{prior_c}{A prior distribution for the dispersion parameter \code{c} (NB only).}

\item{n_iter}{Integer. Total number of MCMC iterations per chain.}

\item{n_chains}{Integer. Number of MCMC chains.}

\item{n_burnin}{Integer. Number of burn-in iterations to discard.}

\item{inits}{Optional. A function or list specifying initial values for the MCMC.}

\item{seed}{Integer. Random seed for reproducibility.}
}
\value{
A named list with the following components:
\describe{
\item{models}{A list of fitted model objects (class \code{rjags}).}
\item{DICs}{A named numeric vector of DIC values for each model.}
\item{best_model}{The model with the lowest DIC.}
}
}
\description{
Fits Poisson, zero-inflated Poisson (ZIP), and negative binomial (NB) models
for underreported count data using JAGS. The function selects the most
parsimonious model based on the Deviance Information Criterion (DIC).
}
